# Notes

The code for our new approach is in the `new_approach` folder. The ablation files are in the `new_approach/ablation` folder. We have a HuggingFace repository that accompanies this one (since some of the files are too large to store on GitHub), which is private for now. 

The DS340W.ipynb file contains the actual code necessary for running our new approach.
